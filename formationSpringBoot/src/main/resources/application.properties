#Use wildcard "*" to expose all endpoints
#Can also expose individual endpoints with a comma-delimited list 

management.endpoints.web.exposure.include=*
management.info.env.enabled=true

#exclude individual endpoints with a comma-demilited list
#management.endpoints.web.exposure.exclude=info,health
info.app.name=Formation Spring Boot App
info.app.description= Acrazy fun App
info.app.version=1.0.0

#Define my crazy properties
coach.name= Mickey Mouse
team.name=The Mouse Club


#Change Spring Boot embedded port
server.port=7070

#Set the context path of the application
#All requests should be prefixes with /mycoolapp

server.servlet.context-path=/mycoolapp